# 🔄 Скрипт обновления сервера

## Использование

После первого деплоя, для обновления кода на сервере выполните:

```bash
cd /var/www/kristal/weebplace
./update-server.sh
```

Или одной командой:

```bash
cd /var/www/kristal/weebplace && ./update-server.sh
```

## Что делает скрипт:

1. ✅ Обновляет код из git (`git pull`)
2. ✅ Устанавливает зависимости бэкенда
3. ✅ Устанавливает зависимости фронтенда
4. ✅ Собирает фронтенд (`npm run build`)
5. ✅ Перезапускает сервис
6. ✅ Проверяет статус и health

## Первая настройка на сервере:

Если скрипта еще нет на сервере, скопируйте его:

```bash
# На вашем локальном компьютере
scp update-server.sh user@89.223.126.245:/var/www/kristal/weebplace/

# На сервере
chmod +x /var/www/kristal/weebplace/update-server.sh
```

Или создайте на сервере вручную:

```bash
cd /var/www/kristal/weebplace
nano update-server.sh
# Вставьте содержимое скрипта
chmod +x update-server.sh
```

## Альтернатива - короткая команда:

Можно добавить алиас в `~/.bashrc`:

```bash
echo 'alias update-weebplace="cd /var/www/kristal/weebplace && ./update-server.sh"' >> ~/.bashrc
source ~/.bashrc
```

Тогда можно просто:
```bash
update-weebplace
```

