# 🚀 План улучшений и новых функций для Weebplace

## 📊 Приоритеты
- 🔴 **Критично** - безопасность, баги, производительность
- 🟡 **Важно** - улучшения UX, масштабируемость
- 🟢 **Желательно** - новые функции, улучшения качества кода

---

## 🔴 КРИТИЧНЫЕ УЛУЧШЕНИЯ

### 1. База данных - Индексы (🔴 Производительность)
**Проблема:** Отсутствуют индексы на часто запрашиваемых колонках, что замедляет запросы.

**Решение:**
```sql
CREATE INDEX IF NOT EXISTS idx_anime_guesses_quiz_date ON anime_guesses(quiz_date);
CREATE INDEX IF NOT EXISTS idx_anime_guesses_user_id ON anime_guesses(user_id);
CREATE INDEX IF NOT EXISTS idx_user_scores_user_id ON user_scores(user_id);
CREATE INDEX IF NOT EXISTS idx_user_scores_quiz_date ON user_scores(quiz_date);
CREATE INDEX IF NOT EXISTS idx_user_scores_quiz_type ON user_scores(quiz_type);
CREATE INDEX IF NOT EXISTS idx_battle_results_user_date ON battle_results(user_id, quiz_date);
CREATE INDEX IF NOT EXISTS idx_openings_quiz_date ON openings(quiz_date);
CREATE INDEX IF NOT EXISTS idx_project_news_created_at ON project_news(created_at);
```

**Файл:** `server/index.js` - добавить после создания таблиц

---

### 2. Race Condition в проверке ответов (🔴 Безопасность/Корректность)
**Проблема:** Если пользователь отправляет два запроса одновременно, может быть засчитано два правильных ответа.

**Текущий код:**
```javascript
// server/index.js:1309-1319
const guessedBy = JSON.parse(guess.guessed_by || '[]');
if (!guessedBy.includes(req.user.id)) {
  guessedBy.push(req.user.id);
  db.run('UPDATE anime_guesses SET guessed_by = ? WHERE id = ?', ...);
}
```

**Решение:** Использовать атомарную операцию SQLite:
```sql
-- Вариант 1: Проверка + обновление в одном запросе
UPDATE anime_guesses 
SET guessed_by = json_set(
  COALESCE(guessed_by, '[]'),
  '$[' || json_array_length(COALESCE(guessed_by, '[]')) || ']',
  ?
)
WHERE id = ? 
  AND NOT EXISTS (
    SELECT 1 FROM json_each(COALESCE(guessed_by, '[]'))
    WHERE json_each.value = ?
  );

-- Вариант 2: Использовать транзакцию с SELECT FOR UPDATE (SQLite не поддерживает, но можно использовать db.serialize)
```

**Альтернатива:** Добавить таблицу `quiz_guesses` вместо JSON в `guessed_by`:
```sql
CREATE TABLE quiz_guesses (
  quiz_id TEXT,
  user_id TEXT,
  guessed_at INTEGER,
  PRIMARY KEY (quiz_id, user_id),
  FOREIGN KEY (quiz_id) REFERENCES anime_guesses(id),
  FOREIGN KEY (user_id) REFERENCES users(id)
);
```

---

### 3. Валидация входных данных (🔴 Безопасность)
**Проблема:** Недостаточная валидация в некоторых endpoints.

**Что добавить:**
- Использовать `express-validator` (уже в dependencies, но не используется)
- Валидация username (только латиница/цифры/подчеркивание, длина)
- Валидация email (если будет добавлена)
- Санитизация всех строковых входов
- Проверка типов данных

**Пример:**
```javascript
const { body, param, query, validationResult } = require('express-validator');

app.post('/api/register', [
  body('username')
    .trim()
    .isLength({ min: 3, max: 20 })
    .matches(/^[a-zA-Z0-9_]+$/)
    .escape(),
  body('password')
    .isLength({ min: 4 })
    .custom((value) => {
      if (value.includes(' ')) throw new Error('Password cannot contain spaces');
      return true;
    })
], async (req, res) => {
  const errors = validationResult(req);
  if (!errors.isEmpty()) {
    return res.status(400).json({ errors: errors.array() });
  }
  // ...
});
```

---

### 4. Логирование в продакшене (🔴 Безопасность)
**Проблема:** Много `console.log` с чувствительными данными.

**Решение:**
- Использовать библиотеку логирования (winston, pino)
- Уровни логирования (error, warn, info, debug)
- Не логировать пароли, токены, персональные данные
- Логи в файл в продакшене

```javascript
const winston = require('winston');
const logger = winston.createLogger({
  level: process.env.LOG_LEVEL || 'info',
  format: winston.format.json(),
  transports: [
    new winston.transports.File({ filename: 'error.log', level: 'error' }),
    new winston.transports.File({ filename: 'combined.log' })
  ]
});

if (NODE_ENV !== 'production') {
  logger.add(new winston.transports.Console({
    format: winston.format.simple()
  }));
}
```

---

## 🟡 ВАЖНЫЕ УЛУЧШЕНИЯ

### 5. Рефакторинг - Разделение на модули (🟡 Архитектура)
**Проблема:** Весь код в одном файле `server/index.js` (1845 строк).

**Решение:** Разделить на модули:
```
server/
├── index.js              # Точка входа
├── config/
│   └── database.js       # Инициализация БД
├── middleware/
│   ├── auth.js           # JWT middleware
│   ├── validation.js     # Валидация
│   └── rateLimit.js      # Rate limiting
├── routes/
│   ├── auth.js           # /api/register, /api/login, /api/me
│   ├── quizzes.js        # /api/anime-guesses, /api/packs
│   ├── scores.js         # /api/scores
│   ├── leaderboard.js    # /api/leaderboard
│   ├── stats.js          # /api/stats
│   ├── news.js           # /api/news
│   ├── library.js        # /api/library
│   ├── battles.js        # /api/battle-*
│   └── openings.js       # /api/openings
├── models/
│   ├── User.js
│   ├── Quiz.js
│   └── Score.js
├── utils/
│   ├── logger.js
│   └── validation.js
└── migrations/
    └── add_indexes.js
```

---

### 6. Кэширование (🟡 Производительность)
**Проблема:** Глобальная статистика и лидерборд вычисляются каждый раз заново.

**Решение:**
- Использовать Redis или in-memory cache (node-cache)
- TTL: статистика - 5 минут, лидерборд - 1 минута
- Инвалидация при обновлении данных

```javascript
const NodeCache = require('node-cache');
const cache = new NodeCache({ stdTTL: 300 }); // 5 минут

app.get('/api/stats/global', async (req, res) => {
  const cacheKey = 'stats:global';
  const cached = cache.get(cacheKey);
  if (cached) return res.json(cached);
  
  // ... вычисление статистики ...
  
  cache.set(cacheKey, result);
  res.json(result);
});
```

---

### 7. Пагинация (🟡 Производительность/UX)
**Проблема:** Некоторые списки возвращают все записи.

**Что добавить пагинацию:**
- `/api/anime-guesses` - список квизов
- `/api/news` - уже есть limit, но нет offset
- `/api/leaderboard` - большие списки
- `/api/openings` - список опенингов

**Формат:**
```javascript
{
  data: [...],
  pagination: {
    page: 1,
    limit: 20,
    total: 150,
    totalPages: 8
  }
}
```

---

### 8. Оптимизация запросов лидерборда (🟡 Производительность)
**Проблема:** Лидерборд парсит JSON из `guessed_by` для всех записей в памяти.

**Решение:** Использовать отдельную таблицу `quiz_guesses` (см. пункт 2) или материализованное представление.

---

### 9. Тестирование (🟡 Качество кода)
**Проблема:** Нет тестов.

**Решение:**
- Unit тесты для utils, models
- Integration тесты для API endpoints
- Использовать Jest или Mocha

```bash
npm install --save-dev jest supertest
```

**Структура:**
```
server/
├── __tests__/
│   ├── routes/
│   │   ├── auth.test.js
│   │   └── quizzes.test.js
│   ├── utils/
│   └── models/
```

---

### 10. API версионирование (🟡 Масштабируемость)
**Проблема:** Нет версионирования API, сложно делать breaking changes.

**Решение:**
```
/api/v1/register
/api/v1/login
/api/v2/register (если нужны изменения)
```

---

## 🟢 ЖЕЛАТЕЛЬНЫЕ УЛУЧШЕНИЯ И НОВЫЕ ФУНКЦИИ

### 11. Поиск пользователей (🟢 Функциональность)
**Эндпоинт:** `GET /api/users/search?q=username`

**Возможности:**
- Поиск по username
- Возвращать только публичную информацию
- Ограничение результатов (max 20)

---

### 12. Система уведомлений на сервере (🟢 Функциональность)
**Проблема:** Уведомления только в localStorage на клиенте.

**Решение:**
- Таблица `notifications` в БД
- Endpoints: `GET /api/notifications`, `POST /api/notifications/:id/read`
- Уведомления о: новых друзьях, ответах на комментарии, достижениях

```sql
CREATE TABLE notifications (
  id TEXT PRIMARY KEY,
  user_id TEXT NOT NULL,
  type TEXT NOT NULL, -- 'friend_request', 'achievement', 'comment'
  title TEXT NOT NULL,
  message TEXT,
  read INTEGER DEFAULT 0,
  payload TEXT, -- JSON
  created_at INTEGER NOT NULL,
  FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE INDEX idx_notifications_user_unread ON notifications(user_id, read);
```

---

### 13. Комментарии к квизам (🟢 Функциональность)
**Эндпоинты:**
- `GET /api/quizzes/:id/comments`
- `POST /api/quizzes/:id/comments`
- `DELETE /api/quizzes/:id/comments/:commentId`

```sql
CREATE TABLE quiz_comments (
  id TEXT PRIMARY KEY,
  quiz_id TEXT NOT NULL,
  user_id TEXT NOT NULL,
  text TEXT NOT NULL,
  created_at INTEGER NOT NULL,
  FOREIGN KEY (quiz_id) REFERENCES anime_guesses(id),
  FOREIGN KEY (user_id) REFERENCES users(id)
);
```

---

### 14. История активности пользователя (🟢 Функциональность)
**Эндпоинт:** `GET /api/users/:id/activity`

**Что сохранять:**
- Угаданные квизы (уже есть в guessed_by)
- Полученные достижения
- Добавленные друзья
- Комментарии

```sql
CREATE TABLE user_activity (
  id TEXT PRIMARY KEY,
  user_id TEXT NOT NULL,
  activity_type TEXT NOT NULL, -- 'quiz_guess', 'achievement', 'friend_add'
  activity_data TEXT, -- JSON
  created_at INTEGER NOT NULL,
  FOREIGN KEY (user_id) REFERENCES users(id)
);
```

---

### 15. Рейтинг сложности квизов (🟢 Функциональность)
**Возможности:**
- Отслеживать процент правильных ответов
- Автоматически определять сложность
- Фильтрация по сложности

```sql
ALTER TABLE anime_guesses ADD COLUMN difficulty_score REAL;
ALTER TABLE anime_guesses ADD COLUMN total_attempts INTEGER DEFAULT 0;
ALTER TABLE anime_guesses ADD COLUMN correct_attempts INTEGER DEFAULT 0;
```

---

### 16. Экспорт данных пользователя (🟢 Функциональность)
**Эндпоинт:** `GET /api/export?format=json`

**Что экспортировать:**
- Библиотеки (watched, favorites, etc.)
- Статистику
- Достижения
- Друзей

**Формат:** JSON или CSV

---

### 17. Восстановление пароля (🟢 Безопасность/UX)
**Эндпоинты:**
- `POST /api/auth/forgot-password` - отправка токена
- `POST /api/auth/reset-password` - сброс пароля

**Требования:**
- Email система (nodemailer)
- Токены срока действия (1 час)
- Таблица `password_reset_tokens`

---

### 18. Email верификация (🟢 Безопасность)
**Возможности:**
- Верификация email при регистрации
- Таблица `email_verifications`
- Endpoint для повторной отправки

---

### 19. Двухфакторная аутентификация (2FA) (🟢 Безопасность)
**Библиотека:** `speakeasy` или `otplib`

**Эндпоинты:**
- `POST /api/auth/2fa/enable` - включение 2FA
- `POST /api/auth/2fa/verify` - проверка кода
- `POST /api/auth/2fa/disable`

---

### 20. WebSocket для real-time обновлений (🟢 UX)
**Использование:** `socket.io` или `ws`

**Применение:**
- Обновление лидерборда в реальном времени
- Уведомления
- Онлайн статус пользователей

---

### 21. Push-уведомления (🟢 UX)
**Библиотека:** Web Push API

**Применение:**
- Уведомления в браузере
- Новые друзья
- Достижения

---

### 22. Аналитика (🟢 Аналитика)
**Что отслеживать:**
- Популярные квизы
- Время прохождения
- Конверсия (сколько начали, сколько завершили)
- Устройства пользователей

**Инструменты:**
- Google Analytics (клиентская)
- Собственная система аналитики

---

### 23. Социальные функции
- **Лайки на комментарии**
- **Ответы на комментарии** (threading)
- **Репосты квизов**
- **Создание коллекций квизов пользователями**

---

### 24. Игровые улучшения
- **Ежедневные челленджи** (специальные квизы)
- **Сезоны** (временные периоды с наградами)
- **Турниры** (соревнования между пользователями)
- **Система бонусов** за streak
- **Разные уровни сложности** в одном квизе

---

### 25. Улучшения производительности фронтенда
- **Ленивая загрузка изображений** (lazy loading)
- **Виртуализация списков** (для больших списков)
- **Service Worker** для офлайн режима
- **Оптимизация bundle size** (code splitting)
- **Мемоизация** вычислений в компонентах

---

### 26. Мониторинг и метрики
- **Health check** улучшить (проверка БД, диска)
- **Prometheus метрики**
- **Sentry** для отслеживания ошибок
- **Uptime мониторинг**

---

### 27. Документация
- **Swagger/OpenAPI** документация API
- **README** с примерами
- **Архитектурная документация**
- **Contributing guide**

---

### 28. CI/CD улучшения
- **Автоматические тесты** в GitHub Actions
- **Автоматический деплой** при merge в main
- **Linting** (ESLint) в CI
- **Pre-commit hooks** (husky)

---

### 29. Безопасность
- **HTTPS/SSL** (Let's Encrypt)
- **CSP headers** (Content Security Policy)
- **Rate limiting** по endpoint (уже есть, но можно улучшить)
- **IP whitelist** для админ endpoints (опционально)
- **Audit log** для админ действий

---

### 30. Масштабируемость
- **PostgreSQL** вместо SQLite (для продакшена)
- **Redis** для сессий и кэша
- **CDN** для статических файлов
- **Load balancing** (если несколько инстансов)
- **Database connection pooling**

---

## 📝 Приоритетный план действий

### Неделя 1: Критичные исправления
1. ✅ Добавить индексы в БД
2. ✅ Исправить race condition в проверке ответов
3. ✅ Добавить валидацию входных данных
4. ✅ Улучшить логирование

### Неделя 2: Важные улучшения
5. ✅ Рефакторинг - разделение на модули
6. ✅ Добавить кэширование
7. ✅ Пагинация для основных endpoints

### Неделя 3: Тестирование и документация
8. ✅ Написать тесты для критичных endpoints
9. ✅ Добавить API документацию (Swagger)

### Неделя 4+: Новые функции
10. Поиск пользователей
11. Система уведомлений
12. Комментарии к квизам
13. И т.д. по приоритету

---

## 💡 Рекомендации по реализации

1. **Начните с критичных пунктов** - они влияют на безопасность и производительность
2. **Рефакторинг делайте постепенно** - не переписывайте всё сразу
3. **Добавляйте тесты параллельно** с новыми функциями
4. **Документируйте изменения** в CHANGELOG.md
5. **Используйте feature branches** для новых функций

---

*Документ создан: 2024-12-22*
*Последнее обновление: 2024-12-22*
